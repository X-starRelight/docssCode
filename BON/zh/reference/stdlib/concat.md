# std.concat

合并两个数组。

## 签名

```
concat(a1: array, a2: array) → array
```

## 说明

返回一个新数组，包含 `a1` 和 `a2` 的所有元素（按顺序）。

## 示例

```bon
std.concat([1], [2])       // [1, 2]
std.concat([1, 2], [3, 4]) // [1, 2, 3, 4]
```

```json
[1, 2, 3, 4]
```
